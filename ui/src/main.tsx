import { createRoot } from "react-dom/client";
import "./index.css";
import MultiLevelSidebar from "./components/SideBar";
createRoot(document.getElementById("render")!).render(<MultiLevelSidebar />);
